컴퓨터 구조


cpu , 시스템 버스 , 주기억장치 , 보조 기억장치, 입출력장치


비트 : 0과1을 표현하는 가장 작은 정보 단위

2의 보수가 음수를 표현하는 방법이다.

2의 보수는 1의 보수에 1을 더한 값이다.

1의 보수 : ex) 1000 -> 0111


cpu는 가산기는 있지만 감산기는 없다!
=> 2의 보수를 통해 음수를 표현한다.


패리티 비트 (오류 검출용 비트) : 문자 표현하는 비트에서 8비트 중 1비트는 패리티 비트이다.

cpu안에 레지스터(임시 기억 장치), 가까운 곳에 캐쉬 메모리

바깥에 RAM (휘발성 메모리)


스택은 프로그램 실행 중 메인을 실행하다 함수를 만나면 그 함수를 실행하면서 사용한다. return을 통해 데이터를 다 사용하고 메인으로 돌아간다.
스택은 메모리 제일 상단에 있음 스택이 많을 수록 데이터의 공간이 부족해지기 때문에 많은 부분을 차지하지 않는다.

cpu : ALU(연산장치) + 제어장치 + 레지스터

레지스터
1 프로그램 카운터 : 메모리에서 가져올 명령어의 주소(메모리에서 읽어 들일 명령어의 주소)
2 명령어 레지스터 : 해석할 명령어(방금 메모리에서 읽어 들인 명령어)
3 메모리 주소 레지스터 : 메모리의 주소
4 메모리 버퍼 레지스터 : 메모리와 주고받을 값(데이터와 명령어)
5 플래그 레지스터
6 범용 레지스터
7 스택 포인터
8 베이스 레지스터 : 기준 주소


오퍼랜드에 상세주소

=> 오퍼랜드 필드의 값 + 레지스터의 값 (변위 주소 지정 방식)
=> 오퍼랜드 필드의 값 + 프로그램 카운터의 값 (상대 주소 지정 방식)

인터럽트

프로세스 => 잘게 쪼개서 스레드로 만든다.


스레드 : 실행 흐름의 단위(일의 단위)

스레드(논리 코어) : 가상의 코어
=>cpu의 유효시간을 줄여 효율 증대


명령어 파이프라인

여러 개의 파이프라인이 있어도 그에 따라 비례해서 처리 속도가 증가하지 않음
=> 명렁어 파이프라인의 위험도 때문에



슈퍼스칼라와 하이퍼스레딩

슈퍼스칼라: 요리사가 쓸수 있는 화구가 4개인 주방( 하드웨어 자원 자체를 늘림)

하이퍼스레딩 : 화구는 1개인데 요리사가 2명인 상황(노는 하드웨어를 없애는 효율 극대화)

명령어 집합 : cpu의 언어

=> 명령어가 달라지면 
해석방식, 레지스터의 종류와 개수, 파이프라이닝의 유용성도 달라진다.

명령어 집합의 두 축
CISC , RISC

CISC : 복잡한 명령어 집합을 활용하는 컴퓨터(cpu)
=> 명령어의 형태와 크기가 다양한 가변 길이 명령어

RISC: 명령어의 종류가 적고 짧고 규격화된 명령어 사용
=>파이프라이닝에 유리하다


메모리에서 데이터를 꺼내는 작업 = fetch


DRAM SRAM

DRAM : 동적 메모리(휘발성 메모리) 
SRAM : 정적 메모리(사라지지 않는 메모리) => DRAM보다 빠르다, 캐시 메모리에서 사용된다.

물리 주소: 메모리 입장에서 바라본 주소(하드웨어상의 주소)

논리 주소: 프로그램 상의 주소(0번지부터 시작)

cpu 성능 향상 기법(MMU)
1. 칩설계(cisc, risc)
2. 클럭의 수를 높이기
3. 코어 수 늘리기
4. 스레드 수 늘리기


인터럽트


클럭 : 디지털 시스템에서 일정한 주기로 반복되는 전기 신호(cpu 클럭)
시스템, cpu, 메모리, gpu, 버스 클럭

클럭의 속도를 높이는 방법
1. 코어 수를 늘리는 방법(듀얼코어, 멀티코어 등)
2. 스레드 수를 늘리는 방법(멀티 스레드 등)

코어 : cpu내에서 실제로 연산을 수행하는 독립적인 처리 단위
코어에는 alu, 제어장치, 레지스터가 포함되어 있다
각 코어는 독립적인 캐시를 가진다

스레드 : 작업의 실행 흐름 단위
멀티스레딩 : 하나의 코어가 두개의 스레드를 동시에 처리하는 기술(코어를 늘린다고 성능이 무한적 증가하지 않음)
=> 병렬 처리랑 연관이 된다.

실행 유닛, 공유의 의미

명령어 병렬처리기법
1.명령어 파이프라이닝
2.수퍼스칼라
3.비순차적 명령어 처리(ROB:재정렬버퍼): 예약 스테이션, rob, 레지스터 리네이밍

MIMD : 병렬처리 컴퓨터의 궁극적인 목표

다중프로세서, 다중컴퓨터 ,클러스터 컴퓨터

문맥전환
1. 타이머 인터럽트 : 타임 슬라이스 만료 시
2. i/o 대기 : 프로세스가 디스크 / 네트워크를 기다려야 할 때
3. 시스템 콜: 프로세스가 os에 서비스 요청시


명령어 파이프라인
인출 -> 해석 -> 실행 -> 결과 저장


명령어와 연산부분(오퍼랜드와 오퍼레이션)

스톨과 포워딩

=================================================================


운영체제란 모든 프로그램은 실행을 위해 자원을 필요로 한다.


자원과 자원 사이의 스케쥴링을 os가 담당한다

하드웨어장치를 사용자가 쉽게 사용하게 해주는 프로그램


운영체제의 발전 목적 
1. 편리성 : 사용자에게 편리한 환경 제공
2. 효율성 : 시스템 성능 향상
3. 제어 서비스 향상 : 시스템 오류 예방(예외처리)


운영 체제의 기능 
1. 자원관리 : 메모리, 프로세스, 주변장치(입출력장치), 파일
2. 시스템 관리 : 시스템보호(사용자 권한 부여), 네트워킹, 명령 해석기


운영체제의 발전과정

스풀링

부팅서비스(부트스트래핑) :운영체제를 메인메모리에 적재하는 과정

이중모드와 시스템 호출

시스템 호출 : 커널모드로 전환하여 실행하기 위해 호출(일종의 소프트웨어 인터럽트)
시스템 호출이 처리되는 방식은 하드웨어 인터럽트 처리 방식과 유사하다

운영체제

1.프로세스의 개념

프로그램과 프로세스

프로세스의 구조 : 코드, 데이터, 힙, 스택

2.프로세스의 상태

PCB : 프로세스 제어 블록

프로그램 + pcb = 프로세스

프로세스의 비활성 상태 : 휴식 상태, 


3. 프로세스 제어블록과 문맥교환

제어블록의 필요성, 구성요소 

문맥교환의 개념, 과정, 문맥교환과 타임 슬라이스의 크기


4. 프로세스의 계층구조

필요성(가비지 컬렉션),

프로세스 생성 시스템 호출

fork(): 자식 프로세스를 생성한다

exec(): 프로세스를 재사용하는 함수

프로세스 동기화
wait()

5. 프로세스의 데이터 영역

데이터 영역

프로세스의 가변적 영역



==================================================
26.03.30

1.스레드와 프로세스

프로세스를 잘라둔것이 스레드이다



멀티태스킹:  프로세스 간 통신 사용

멀티스레딩:  


멀티스레드의 구조
멀티태스킹과 멀티스레드의 구조적 차이

멀티스레드의 구조 : 단일스레드, 멀티스레드(병행처리 가능)

병렬처리 용어 : 멀티태스킹(시분할), 멀티스레드, 멀티프로세싱(cpu가 여러개), cpu 멀티스레딩(cpu는 1개인데 스레드가 여러 개)

멀티스레드의 장점
자원공유
문맥 교환
응답 속도
적응성


단점
모든 스레드가 프로세스와 함게 종료(은행 앱(프로세스), 스레드1(사용자 ui처리), 스레드2(송금처리), 스레드3(로그 기록)) : 한개만 오류가 발생하여 프로세스가 강제 종료되면 스레드도 전부 종료된다


2.스레딩

멀티스레딩 모델(커널 스레드와 사용자 스레드와의 관계)
- 1:N : 여러 개의 사용자 스레드가 하나의 커널 스레드를 사용  => 멀티코어 환경에서 잘 안쓴다
낮은 문맥교환 비용, 입출력 대기 비효율, 멀티코어 활용 불가능, 보안 취약
- 1:1 : 사용자 스레드 1개가 커널 스레드 1개에 매핑되는 구조
높은 문맥 교환 비용, 안정성(공유 변수 보호 기능 제공), 입출력 대기 효율, 멀티코어 활용 가능
- M:N : 멀티 레벨 스레딩
높은 문맥 교환 비용, 입출력 대기 유연성, 멀티코어 활용 가능, 스케줄링 및 동기화의 복잡성


3.프로세스 간 통신

프로세스 간 통신의 개념
- 프로세스 간 통신(ipc) : 프로세스가 서로 연결을 주고받는 것
- 운영체제는 프로세스끼리 데이터를 주고받도록 다양한 프로세스 통신기법 제공

프로세스 간 통신 기법의 종류
-파이프 통신, 소켓통신, 공유 메모리 및 공유 파일(프로세스가 직접 처리)
-파이프와 소켓은 운영체제가 처리한다

파이프통신
-단방향 통신 : 프로세스 A-> B(파이프1) , 프로세스 B -> A (파이프 2) : 별도의 파이프 필요
-같은 컴퓨터 안에서만 가능
-부모 자식 프로세스간 주로 사용

소켓 통신
-양방향 통신
-다른 컴퓨터와도 통신 가능
-ip+포트번호로 상대방을 찾음(192.168.0.1 : 8080)


비교
속도는 파이프가 빠르고 소켓이 느림
구현 난이도도 파이프가 쉽고 소켓이 복잡함
사용처 파이프는 부모-자식 프로세스, 소켓은 네트워크통신, 서버-클라이언트


동기화 통신과 비동기화 통신
- 비동기화 통신 : 데이터 도착 시점을 알 수 없어, 프로세스가 반복문을 통해 도착 여부를 직접 확인(폴링, 블로킹)

- 동기화 통신 : 동기화 통신은 운영체제가 데이터 도착까지 자동으로 대기시킴, 블로킹 방식의 통신


osi 7 layer(7계층)
물리(케이블, 광섬유) , 데이터링크(이더넷, wi-fi), 네트워크(ip, arp), 전송(tcp,udp), 세션(netbios, rpc), 표현(ssl/tls, utf-8), 응용(http, ftp,dns,smtp)

tcp 와 udp

연결방식 : 연결후 전송, 바로 전송
신뢰성 : 높음(재전송), 낮음(손실 허용)
속도 : 느림, 빠름
순서보장: 보장, 미보장
사용예시 : 웹, 이메일     게임,스트리밍
비유 : 등기우편, 일반 전단지 배포

네트워크 통신 이해하기
ip : 데이터를 목적지까지 전송하기 위해 개발된 통신 규약
tcp : 네트워크를 통해 받은 데이터를 정리, 인터페이스의 역할을 하는 통신 규약

포트번호 : tcp가 네트워크를 사용하는 프로세스를 구분하기 위해 사용하는 주소
소켓 프로그래밍 : 하나의 포트에 여러 클라이언트가 동시에 접속할 수 있도록 소켓이 중간에서 연결을 관리하는 통신방식



프로세스 스케줄링

1. cpu 스케줄링의 개요

1-1cpu 스케줄링 : 운영체제가 프로세스들에게 공정하게 cpu자원 분배

- 단계별 스케줄링 : 작업대기 -> 보류 프로세스 -> 활성 프로세스 -> 실행프로세스 -> 작업종료
고수준, 중수준, 저수준 스케줄링
디스패치:

- 선점형 스케줄링, 비선점형 스케줄링
선점형 : 스케줄러가 실행중인 프로세스 cpu를 강제로 빼앗아 다른 프로세스에 할당할 수 있는 스케줄링 방식(시분할 시스템, 인터럽트)
비선점형 : 프로세스가 cpu를 사용하면 작업이 끝날 때까지 빼앗을 수 없는 스케줄링 방식(일괄 작업 시스템)

선점형, 비선점형 비교

장점 : 빠른 응답을 요구하는 시스템에 적합, 문맥교환과 같은 부가적인 작업이 없다.
단점 : 문맥 교환의 오버헤드가 많다, 실행 시간이 긴 작업 때문에 실행시간이 작은 작업이 오래 기다려서 처리율이 떨어진다.
우선순위 : 높다, 낮다

cpu스케줄링의 목표
-공평성
-효율성
-안정성
-확장성
-반응 시간 보장
-무한 연기 방지


1-2우선순위와 cpu 스케줄링

입출력 작업이 많은 프로세스의 우선순위는 cpu작업이 많은 프로세스의 우선순위보다 높다

전면프로세스와 후면 프로세스
-전면: 그래픽 사용자 인터페이스 환경에서 맨 앞에서 실행되는 프로세스(상호작용 프로세스)
-후면: 사용자와의 상호작용이 없는 일괄 작업 프로세스(압축, 디스크 백업, 자동 업데이트)


프로세스의 우선순위를 판단하는 기준
커널 <-> 사용자
전면 <-> 후면
대화형 <-> 일괄작업
입출력 집중 <->  cpu 집중

고정 우선순위와 변동 우선순위

준비 큐와 대기 큐

준비큐는 우선순위에 따라 여러 줄로 구성된다

2. cpu 스케줄링 알고리즘

2-1 알고리즘 종류와 평가

cpu 스케줄링 알고리즘 종류(구별할수 잇으면 된다)
-비선점형
 
-선점형

비선점형, 선점형
우선순위 미적용
선입선출(비선형), 라운드 로빈(선형)

우선순위 적용
최단작업우선, 최고 응답 우선(비선형)    	최소 잔여시간, 다단계 큐, 다단계 피드백 큐(선형)


평가 기준
-cpu 사용률
-처리량
-대기 시간
-응답 시간
-반환 시간


2-2 선입선출 스케줄링

일괄작업 시스템
콘보이 효과 : 짧은 프로세스가 도착해도 오랫동안 기다려야하는 문제가 발생할 수 있다

2-3 최단 작업 우선 스케줄링

실행 시간이 짧은 작업부터 cpu를 할당하는 스케줄링
종료시간 예측의 어려움
공평성 위배
기아 현상 : 특정 프로세스가 cpu나 자원을 계속 할당받지 못하는 상태
에이징(기아현상의 해결책) : 프로세스가 양보할 수 있는 상한선을 정하는 기법

2-4 최고 응답 우선 스케줄링(HRN)
기아현상을 완화하는 비선점형 알고리즘
대기 시간과 실행 시간을 모두 고려하여 우선순위를 결정

2-5 라운드 로빈 스케줄링(RR)
모든 프로세스가 일정한 시간(타임 슬라이스) 동안만 cpu를 사용하는 방식
순환 순서 방식
작업이 끝나지 않으면 준비 큐의 맨뒤로 이동

2-6 최소 잔여 시간 스케줄링(SRT)
남아있는 작업 시간이 더 적은 프로세스에 먼저 cpu 할당
최단 작업 우선 스케줄링을 선점형으로 변환한 것

2-7 다단계 큐(MLQ) 스케줄링
단점 : 하위 큐에 있는 우선순위가 낮은 프로세스의 실행이 계속 지연
각 큐는 라운드 로빈 방식을 운영
작업 효율성 증가

2-8 다단계 피드백 큐(MLFQ) 스케줄링
우선순위가 동적으로 변하는 변동 우선순위 방식
다단계 큐 스케줄링의 단점을 완화하는 방식

===============================================

2026.04.06


12장 프로세스 동기화

병행 프로세스(base: 공유 자원)
os가 프로세서를 빠르게 전환, 프로세서 시간 나눠 마치 프로세스를 여러개 동시에 실행하는 것처럼 보이게 하는 것

- 여러 프로세스가 동시에 실행되는 것
- 순서나 타이밍이 보장되지 않으며, 서로 독립적으로 진행
- 비동기적으로 동작
- 메모리 공유 영역

동기적 프로세스
-프로세스들이 순서와 타이밍을 맞춰 실행되는 것 : 순차적 실행
- 한 작업이 끝나야 다음작업이 시작되거나 특정 시점에 서로 보조를 맞추는 방식

비동기적 프로세스
- 병렬적 실행 가능
- 기다리지 않고 즉시 다음 단계 진행



프로세스 동기화
- 여러 프로세스가 공유자원에 동시에 접근할때 발생하는 문제를 해결하고 올바르게 제어하는 메커니즘
- 상호배제(공유 자원 보호)와 실행 순서 제어가 목적임

-핵심 개념 : 경쟁 조건 : 두 개 이상의 프로세스가 공유 데이터에 동시에 접근하면 실행순서에 따라 결과가 달라지는 문제 발생

- 동기화가 사용되는 목적
1. 상호배제와 실행 순서 제어가 목적임
2. 병행 프로세스이므로 동기화가 필요한 것


병행 프로세스 종류
1. 독립 프로세스 
-단일 프로그래밍
-다중 프로그래밍 : 프로세스 여러 개가 프로세서 하나 공유
-다중 처리 : 프로세서(cpu) 2개 이상 사용하여 동시에 프로그램 여러 개를 병렬 실행

2. 협력 프로세스: 다른 프로세스와 상호작용하며 특정 기능을 수행하는 비동기적 프로세스

병행 프로세스의 해결과제

- 병행성
1. 여러 프로세스를 이용하여 작업을 수행하는 것
2. 시스템 신뢰도 높이고 처리 속도 개선, 처리 능력 높이는데 중요

병행성은 문제를 해결해야한다
1. 공유 자원을 상호 배타적으로 사용해야한다 예를 들어 , 프린터, 통신만 등은 한순간에 프로세스 하나만 사용해야 한다
2. 병행 프로세스 간에는 협력이나 동기화가 되어야 한다. 상호배제도 동기화의 한 형태이다
3. 두 프로세스 사이에서는 데이터를 교환할 수 있도록 통신이 되어야 한다.
4. 프로세스는 동시에 수행하는 다른 프로세스의 실행 속도와 관계없이 항상 일정한 실행 결과를 보장하도록 결정성을 확보해야 한다.
5. 교착 상태를 해결하고 병행 프로세스들의 병렬 처리 능력을 극대화 해야 한다.
6. 실행 검증 문제를 해결해야 한다.
7. 병행 프로세스를 수행하는 과정에서 발생하는 상호배제, 즉 어떤 프로세스가 작업을 실행 중일 때 나머지 프로세스는 그것돠 관련된 작업을 수행할 수 없도록 보장해야한다.

=> 선행 그래프를 이용하여 프로세스들이 운영체제의 지원 없이 상호배제를 책임지는 소프트웨어적이 방법

상호배제를 책임지는 소프트웨어적인 방법
1. 선행 그래프
2. fork, join
3. 병행 문장(parbegin/ parend)

1.선행 그래프의 개념

-프로세스는 프로세스 집합과 이것의 선행 제약으로 정의할 수 있다.

-선행 제약 : 프로세스를 순서대로 다른 상태에 옮기는 것
   		만약 두 프로세스에 선행 제약이 없으면 이 둘은 독립적이라서 병행 실행이 가능하다

-선행 그래프: 선행 제약을 논리적으로 표현한 것
=> 순차적 활동을 표현하는 방향성 비순환 그래프이다.

병행 프로그램을 선형 그래프로 표현하는 이유
- 순차 프로그램은 코드 흐름을 따른다
- 병행 프로그램은 여러 프로세스가 동시에 실행되므로 순서 예측이 어려움
- 선형 그래프 사용 이유
1. 가능한 실행 경로의 전체 공간을 볼 수 있음
2. 임계구역의 위치를 직사각형으로 표시 가능
3. 안전한 실행 경로와 위험한 경로 구분 가능

2.fork, join 구조

프로그램 선행 관계를 명시하려면 fork,join 구조나 병행문장이 필요하다

fork : 단일 연산을 독립 연산 2개로 분할
join : 병행 연산 2개를 하나로 결합

=> 병행 프로그램을 작성하는 데 매우 효과적이지만 , 제어 구조(프로그램이 실행되는 순서와 방식)는 어색하다

3.병행 문장

병행 문장은 하나의 프로세스가 여러 병렬 프로세스로 퍼졌다가 다시 하나로 뭉쳐지는 것을 나타내는 고급 언어 구조

=> 블록 구조의 고급 언어에 쉽게 추가할 수 있으며, 다른 구조적 제어 문장의 장점을 많이 보여준다.

상호배제와 동기화

상호배제 : 병행 프로세스에서 프로세스 하나가 공유 자원을 사용할 때 다른 프로세스들이 동일한 일을 할 수 없도록 하는 방법
공유 자원에 있는 데이터에 접근하는 다른 프로세스를 프로세스 하나가 해당 데이터에 접근할 수 없게 하는 것

예를 들어 변수, 파일은 하나씩 차례대로 써야 한다
=> 공유 자원을 동시에 사용하지 못하게 실행을 제어하는 방법 : 동기화

동기화로 상호배제를 보장할 수 있지만 이 과정에서 교착상태와 기아상태가 발생할 수 있다

*동기화에는 상호 배제말고도 순서 정하기라는 개념이 포홤되어 있다

임계 자원: 두 프로세스가 동시에 사용할 수 없는 공유 자원
임계 영역: 임계 자원에 접근하고 실행하는 프로그램 코드 부분


상호배제의 조건
- 두 프로세스는 동시에 공유 자원에 진입 불가
- 프로세스의 속도나 프로세서 수에 영향 받지 않음
- 공유 자원을 사용하는 프로세스만 다른 프로세스 차단 가능
- 프로세스가 공유 자원을 사용하려고 너무 오래 기다려서는 안 됨(기아상태)



뮤텍스 락
-상호배제의 줄임말
-여러 스레드가 공유자원에 동시에 접근하는 것을 막기 위한 동기화 메커니즘
-경쟁 조건이 발생 시 한 번에 하나의 스레드만 임계구역에 진입하도록 보장


임계영역
다수의 프로세스 접근 가능하지만, 어느 한 순간에는 프로세스 하나만 사용 가능

임계구역이 만족해야 할 3가지 조건
1. 상호배제
2. 진행
3. 한정 대기
* 임계구역 범위는 최소화해야 함

생산자, 소비자 문제와 상호배제를 해결하는 초기의 시도

생산자 - 소비자 문제
운영체제에서 비동기적으로 수행하는 모델
- 생산자 : 데이터를 만들어 버퍼에 넣는 것
- 소비자 : 버퍼에서 데이터를 꺼내 처리하는 것

생산자 -> 버퍼 -> 소비자

발생하는 문제
1. 버퍼가 가득 찼을 때
2. 버퍼가 비었을 때
3. 동시 접근 문제 

경쟁상태 : 여러 프로세스가 동시에 공유 데이터에 접근할 때 접근 순서에 따라 실행 결과가 달라지는 상황에 놓인 프로세스

상호배제를 해결하는 방법

소프트웨어로 해결: 데커, 다익스트라
소프트웨어가 제공(운영체제 및 프로그래밍 수준 도구) : 세마포, 모니터, 뮤텍스
하드웨어로 해결 : 테스

모니터 : 사용자 사이에서 통신하려고 동기화하고, 자원에 배타적으로 접근할 수 있도록 프로세스가 사용하는 병행 프로그래밍 구조
 
세마포어: 정수 변수 하나로 표현
p연산 : 대기, 정수 값을 1감소. 프로세스가 자원을 사용하려 할 때 호출
v연산: 프로세스 깨움. 정수값을 1증가. 프로세스가 자원 사요을 마쳤을 떄 호출
지금 몇 개의 프로세스가 더 들어올 수 있는지를 나타내는 숫자
숫자의 증감으로 프로세스의 접근과 대기를 제어함

================================================

2026.04.09

컴퓨터 구조

13장 교착상태

process - resource

1.교착상태의 개념과 발생 원인(리소스를 원하는 모든 프로세스에 영향을 준다)
- 프로세스가 교착 상태에 빠지면 작업이 정지되어 명령 진행 불가
- 운영체제가 교착 상태를 해결하지 못하면, 시스템 운영자나 사용자는 작업 교체, 종료하는 외부 간섭으로 해결해야 함
- 두 프로세스가 사용하는 자원(비공유) 서로 기다리고 있을 때 발생
- 하나 이상의 작업에 영향을 주어 무한 대기, 기아 상태보다 더 심각한 문제 야기
- 자원 해제 요청 받아들일 때까지 프로세스들은 작업 진행 불가
- 자원 해제 수신 때까지 현재 보유 자원도 해제 불가

1-1 교착 상태의 개념
-프로세스의 자원 사용 순서
1 자원 요청 : 해당 자원을 다른 프로세스가 사용 중이면 요청을 수락 때까지 대기
2 자원 사용 : 프로세스가 요청한 자원 획득하여 사용
3 자원 해제 : 프로세스가 자원 사용 마친 후 해당 자원 되돌려(해제) 줌

1-2 교착 상태의 예

*스풀링은 **"성격 급한 CPU가 느릿느릿한 프린터를 기다려주지 않고, 일단 디스크라는 창고에 물건을 다 던져놓은 뒤 자기 할 일을 하러 가는 시스템"**이라고 이해하시면 가장 정확합니다.

- 스풀링 시스템에서 발생하는 교착 상태
출력장치에 쓰는 스풀링 시스템
디스크에 할당된 스풀 공간의 출력이 완료하지 않은 상태에서 다른 작업이 스풀 공간을 모두 차지하면 발생

- 디스크를 공유할 때 발생하는 교착 상태
제어가 없으면 프로세스들이 서로 충돌하는 명령 요청할 때 교착 상태 발생

-네트워크에서 발생하는 교착 상태
네트워크가 붐비거나 입출력 버퍼 공간이 부족한 네트워크 시스템에 메시지 흐름 제어하는 적절한 프로토콜이 없으면 발생한다.


1-3 발생 조건(1~3만 만족해도 교착 상태가 발생 가능, 발생하지 않을 수도 있음, 4는 1~3조건을 만족할 때 발생할 수 있는 결과, 점유와 대기 조건을 포함하므로 네 가지 조건이 모두 독립적인 것은 아님)

1. 상호배제: 한 번에 프로세스 하나만 해당 자원을 사용할 수 있어야 함
2. 점유와 대기: 자원을 최소한 하나 정도 보유, 다른 프로세스에 할당된 자원 얻으려고 대기하는 프로세스 있어야함
3. 비선점: 자원 선점 불가. 즉 자원은 강제로 빼앗을 수 없고, 자원 점유하고 있는 프로세스 끝나야 해제
4. 순환 대기 : 무조건 사이클이 있다고 교착상태가 나타나는 것은 아니다

1-4 교착 상태의 표현

시스템 자원 할당 그래프인 방향 그래프 표현
G(v,e) : v는 프로세스, 자원 e는 간선 집합

프로세스 -> 리소스 : 자원 요청
리소스 -> 프로세스 : 자원 할당

=> 사이클이 있는 그래프는 교착 상태일 확률이 높다
자원 할당 그래프에 사이클이 없다면 교착 상태가 아님
그러나 사이클이 있으면 시스템은 교착 상태일 수도 있고 아닐 수도 있음

2. 교착 상태를 해결하는 예방,회피,탐지,회복

교착 상태 해결 방법 3가지
1.예방
2.회피
3.탐지, 회복


2-1 예방
하벤더의 교착 상태 예방 방법
각 프로세스는 필요한 자원을 한 번에 모두 요청, 제공받기 전까지 작업 진행 불가
어떤 자원을 점유하고 있는 프로세스의 요청을 더 이상 허용하지 않음, 점유한 자원을 모두 반납, 필요하면 다시 자원 요청
모든 프로세스에 자원을 순서대로 할당

자원의 상호배제 조건 방지
-상호배제는 자원의 비공유가 전제 되어야 함
- 일반적으로 상호배제 조건을 만족하지 않으면 교착 상태 예방 불가능

점유와 대기 조건 방지
-발생하지 않으려면 프로세스가 작업 수행 전에 필요한 자원 모두 요청하고 획득해야 함
-점유와 대기 조건 방지 방법
1. 자원 할당 시 시스템 호출된 프로세스 하나를 실행하는 데 필요한 모든 자원 먼저 할당, 실행 후 다른 시스템 호출에 자원 할당
2. 프로세스가 자원을 전혀 갖고 있지 않을 때만 자원 요청할 수 있도록 허용하는 것. 더 요청하려면 자신에게 할당된 자원을 모두 해제
=> 효율성이 낮음, 기아 상태 발생 가능

비선점 조건 방지
- 이미 할당된 자원에 선점권이 없어야 한다는 전제 조건이 필요
- 대기 상태면 자원을 회수당하고 프로세스가 다시 시작할때는 요청한 새로운 자원과 해제한 자원을 확보
- 두 프로세스에 우선순위를 부여하고 높은 우선순위의 프로세스가 자원을 선점하여 해결
1.프로세서 레지스터나 기억장치 레지스터와 같이 쉽게 저장되고 이후에 다시 복원하기 쉬운 자원에 사용
2.프린터, 카드 판독기, 테이프 드라이버 같은 자원에는 적합하지 않다

순환대기 조건 방지
- 프로세스들이 원형으로 서로의 자원을 기다리는 상태
p1->r2(대기)
p2->r3(대기)
p3->r1(대기)  무한 대기 발생(교착상태)
- 모든 자원에 순서를 부여하고, 프로세스는 반드시 오름차순으로만 자원을 요청하도록 강제한다
=> 원형 대기 구조가 형성 되지 않음(계층적 요청 방법으로 circular wail(순환 대기)의 가능성을 제거
- 오름차순 요청 규칙 : 프로세스는 현재 보유한 자원의 번호보다 큰 자원만 요청 가능
- 순환대기 방지 됨 : 마지막 프로세스는 첫번째 프로세스의 자원 요청 불가

2-2 회피

회피의 개념
-목적 : 덜 엄격한 조건 요구하여 자원 좀 더 효율적 사용
- 예방보다는 회피가 더 병행성 허용
- 가능성이 있으면 인정하고(3가지 필요조건 허용), 회피하는 것(발생 가능성을 미리 제거하는 것이 아님)
- 회피 방법
1. 프로세스의 시작 중단
=> 자원을 언제 요청하는지 정보가 필요. 각 프로세스마다 요청과 해제에서 정확한 순서를 파악하고 있으면, 요청에 따른 프로세스 대기 여부 결정 가능
2. 자원 할당 거부(알고리즘)
=> 다익스트라의 은행가 알고리즘 이용
각 프로세스에 자원을 어떻게 할당(자원 할당 순서 조정) 할 것인지 정보가 필요하므로 각 프로세스가 요청하는 자원의 종류의 최대 수를 알아야 함
-> 교착 상태 회피 알고리즘 정의 가능. 은행에서 모든 고객이 만족하도록 현금 할당하는 과정과 동일

=> 회피 알고리즘은 순환대기 조건이 발생하지 않도록 
자원할당 상태 검사(사용 가능한 자원수, 할당된 자원 수, 프로세스들 최대 요청 수)
각 프로세스에 자원을 할당할 수 있고 교착 상태를 예방할 수 있으면 안정된 상태. 시스템에 안정 순서가 있으면 안정 아니면 불안정


시스템의 상태
-안정상태와 불안정 상태로 구분
-교착상태는 불안정 상태에서 발생
-불안정상태 -> 교착상태 : 무조건은 아니다
-상태가 안정하다면 운영체제는 불안정 상태와 교착 상태를 예방 가능
-불안정 상태의 운영체제는 교착 상태 발생시키는 프로세스의 자원 요청 방지 불가

2-3 회복

회복에 필요한 알고리즘
- 시스템 상태 검사하는 교착 상태 탐지 알고리즘
- 회복시키는 알고리즘

특징
- 언제 수행해야 하는지 결정하기 어려움
- 탐지와 회복 방법은 필요한 정보를 유지하고 탐지 알고리즘을 실행시키는 비용뿐 아니라 교착 상태 회복에 필요한 부담까지 요청
- 탐지를 자주 실행하면 성능이 떨어진다 하지만 빨리 발견하여 유휴 상태(작업이 없어서 쉬는 상황)를 방지할 수 있다.

교착상태 탐지 알고리즘
- 사후에 탐지하고 복구하는 방식
- case1. 자원이 단일 인스턴스일 때 -> 대기 그래프

- case2. 자원이 다중 인스턴스일 때 -> 탐지 알고리즘
=> 현재 상태에서 완료 가능한 프로세스를 찾음

탐지 알고리즘 실행 시점
- 주기적 실행
- 자원요청 실패 시마다
- cpu 사용률 기반

탐지 후 복구 방법
- 프로세스 강제 종료 => 작업 손실
- 자원 선점(다른 프로세스를 희생해서 자원 강제 회수) => 기아 상태가 발생할 수 있다

3. 기아 상태
작업이 결코 사용할 수 없는 자원을 계속 디라기는 결과(교착상태)를 예방하려고 자원을 할당할 때 발생(기다림)하는 결과
교착상태의 해결책은 기아 상태의 가능성을 제거 못함
기아 상태 해결은 먼저 기다리는 작업을 발견하고 각 작업이 기다린 시간을 조사,추적해야 함
시스템은 기아 상태를 발견하면 즉시 새로운 작업의 시작을 대기 하도록 조치해야 함



===================

14 메모리 관리


1-1 메모리 이해하기

메모리 주소
-cpu는 메모리에 있는 내용을 가져오거나 작업 결과를 메모리에 저장하기 위해 주소를 사용한다

1-2 메모리 관리의 이중성

메모리 관리의 복잡성
-일괄처리 시스템은 한번에 하나의 작업만 처리하므로 메모리 관리가 단순함
-시분할 시스템에서는 운영체제를 포함한 모든 응용 프로그램이 메모리에 올라와 실행되기 때문에 메모리 관리가 복잡함

메모리 관리의 이중성
-프로세스 입장에서는 메모리를 독차지하려 하고, 메모리 관리자 입장에서는 되도록 관리를 효율적으로 하고 싶어하는 성질

1-3 메모리 관리 목적
- 효율 : 제한된 메모리를 낭비없이 사용
- 안정성 : 프로그램 간 메모리 침범 방지
- 공정성 : 프로그램 간 메모리 침범 방지
- 성능향상 : 실행 속도와 응답성 개선

메모리 관리 작업 정책
1. 메모리 가져오기(fetch) : 실행할 프로세스와 필요한 데이터를 보조기억장치에서 주기억장치로 가져오는 작업(언제 메모리로 가져올 것인지가 핵심이다)
2. 메모리 배치(placement) : 가져온 프로세스와 데이터를 메모리 어떤 위치에 올려 놓을지 결정(효율적으로 공간을 사용하고, 단편화를 줄이는 것이 목표)
3. 메모리 재배치(replacement) : 메모리가 가득찼을 때 새로운 프로세스나 데이터를 가져오기 위해 기존의 일부를 제거하는 작업(제거하면 보통 보조기억장치로 감)



2 메모리 주소

2-1 32bit cpu와 64bit cpu의 차이
-32bit(주소를 32비트로 표현)
가상주소 -> 물리주소 변환 범위가 제한됨

-64bit(주소를 64비트로 표현)
가상주소 공간이 넓어 프로세스 격리 용이

2-2 논리 주소와 물리 주소
메모리 영역 구분 
- 단순 메모리 구조는 한 번에 한 가지 일만 처리하는 일괄 처리 시스템에서 볼 수 있음
- 메모리를 운영체제(위에 있음) 영역과 사용자 영역(아래에 있음)으로 나누어 관리

=================================================================

2026.04.13

운영체제

가상 메모리(물리 메모리 관리)

1-1 메모리 이해하기

메모리 주소
-cpu는 메모리에 있는 내용을 가져오거나 작업 결과를 메모리에 저장하기 위해 주소를 사용한다

1-2 메모리 관리의 이중성

메모리 관리의 복잡성
-일괄처리 시스템은 한번에 하나의 작업만 처리하므로 메모리 관리가 단순함
-시분할 시스템에서는 운영체제를 포함한 모든 응용 프로그램이 메모리에 올라와 실행되기 때문에 메모리 관리가 복잡함

메모리 관리의 이중성
-프로세스 입장에서는 메모리를 독차지하려 하고, 메모리 관리자 입장에서는 되도록 관리를 효율적으로 하고 싶어하는 성질

1-3 메모리 관리 목적
- 효율 : 제한된 메모리를 낭비없이 사용
- 안정성 : 프로그램 간 메모리 침범 방지
- 공정성 : 프로그램 간 메모리 침범 방지
- 성능향상 : 실행 속도와 응답성 개선

메모리 관리 작업 정책
1. 메모리 가져오기(fetch) : 실행할 프로세스와 필요한 데이터를 보조기억장치에서 주기억장치로 가져오는 작업(언제 메모리로 가져올 것인지가 핵심이다)
2. 메모리 배치(placement) : 가져온 프로세스와 데이터를 메모리 어떤 위치에 올려 놓을지 결정(효율적으로 공간을 사용하고, 단편화를 줄이는 것이 목표)
3. 메모리 재배치(replacement) : 메모리가 가득찼을 때 새로운 프로세스나 데이터를 가져오기 위해 기존의 일부를 제거하는 작업(제거하면 보통 보조기억장치로 감)



2 메모리 주소

2-1 32bit cpu와 64bit cpu의 차이
-32bit(주소를 32비트로 표현)
가상주소 -> 물리주소 변환 범위가 제한됨

-64bit(주소를 64비트로 표현)
가상주소 공간이 넓어 프로세스 격리 용이

2-2 논리 주소와 물리 주소
1.메모리 영역 구분 
- 단순 메모리 구조는 한 번에 한 가지 일만 처리하는 일괄 처리 시스템에서 볼 수 있음
- 메모리를 운영체제 영역(위에 있음)과 사용자 영역(아래에 있음)으로 나누어 관리

**주기억장치에 데이터가 있으면 괜찮지만 없으면 폴트가 일어난다(folt)
=> cpu는 중단되고 보조메모리에서 주기억장치로 메모리를 올려준다

2. 단순 메모리 구조에서 사용자 프로세스 적재
- 사용자 프로세스는 운영체제 영역을 피하여 메모리에 적재
- 사용자 프로세스를 메모리의 최상위부터 사용
=> 그러나 메모리를 거꾸로 사용하기 위해 주소를 변경하는 일이 복잡하기 때문에 잘 쓰이지 않음

3. 경계 레지스터 : os의 영역을 침범하지 못하게 하는 것

4.물리주소
메모리 입장에서 바라본 주소
컴퓨터에 꽂힌 램 메모리 실제 주소

5.논리주소
사용자가 베이스 주소부터 0번지로 생각하고 주소를 표현한다 

6.재배치 레지스터
프로세스의 물리주소를 저장하는 레지스터(논리주소 + 기준주소)
메모리 보호와 주소 변환의 핵심 장치

3 단일 프로그래밍 환경의 메모리 할당

3-1 메모리 오버레이

메모리 오버레이
프로그램의 크기가 실제메모리보다 클 때 전체 프로그램을 메모리에 가져오는 대신 적당한 크기로 잘라서 가져오는 기법
=> 한 개의 프로그램을 나눠서 저장하는 기법

!!!!!그러나 오버레이처럼 조각내서 일부만 실행할 수 없는 상황이라면 가상메모리 기법을 사용한다.

메모리 오버레이 작동 방식
프로그램이 실행되면 필요한 모듈만 메모리에 올라와 실행
프로그램 전체가 아니라 일부만 메모리에 올라와도 실행 가능

3-2 스왑

스왑 영역
하드디스크나 ssd의 일부를 메모리처럼 사용하기 위해 확보한 전용 영역
사용자는 실제 메모리와 스왑 영역의 크기를 합쳐서 전체 메모리로 인식, 사용
RAM의 부족을 보완하기 위한 '임시 대기 공간'
저장장치는 장소만 빌려주고 메모리 관리자가 관리


스왑인 : 스왑 영역에서 메모리로 데이터를 가져오는 작업
스왑아웃: 메모리에서 스왑 영역으로 데이터를 내보내는 작업

04 다중 프로그래밍 환경의 메모리 할당

4-1 메모리에 여러 개의 프로세스를 배치하는 방법
-가변 분할 방식
프로세스의 크기 따라 메모리 나누는 것
메모리의 영역이 각각 다름
연속 메모리 할당
외부 단편화가 발생 => 메모리 배치 방식 -> 조각모음(작은 조각을 모아 하나의 큰 덩어리로 만드는 작업) 순서로 처리한다
세그먼테이션 메모리 관리 기법이라고 함
=>프로세스를 하나의 연속된 주소로 다룬다

-고정 분할 방식
같은 크기로 메모리 나누는것(사용자 관점 X)
큰 프로세스가 메모리에 올라오면 여러 조각으로 나누어 배치
비연속 메모리 할당
페이징 메모리 관리 기법
내부단편화가 발생
고정분할방식에서 적정양보다 큰 데이터가 들어오는 경우
=>나눠서 들어간다 , 연결할 수 있게 링크를 걸어준다


-세그먼테이션 : 가변적인 메모리 논리 공간, 가변 분할 방식의 메모리 관리 기법
-함수
-모듈

-내부단편화(내부에서 사용하지 못하는 공간)
-외부단편화(가장마지막에 남는 공간, 공간과 공간사이에 생김)
=> 해결하기위해 먼저 메모리 배치 방식으로 선처리를 하고 그 다음에 마지막에 조각 모음을 후처리로 한다.
메모리 배치 방식: 작은 조각이 발생하지 않도록 프로세스를 배치하는 것
-조각모음: 작은 조각을 모아 하나의 큰 덩어리로 만드는 작업(주기적으로 실행) => 프로세스가 동작을 멈춤(주소값이 전부 바뀜)

4-2 가변 분할 방식의 메모리 관리

-메모리 배치 방식
최초 배치(최초 적합):첫번째 발견한 공간에 프로세스를 배치(빠르게 메모리를 찾는다)
최적 배치(최적 적합):빈공간을 모두 확인후 가장 알맞은 곳으로 배치
최악 배치(최악 적합):빈공간을 모두 확인후 가장 큰 공간에 프로세스를 배치(그 공간에 여러개의 프로세스를 넣는다는 방식)

4-3 고정 분할 방식의 메모리 관리
일정하게 나뉜 메모리의 크기보다 작은 프로세스가 배치될 경우 낭비 공간이 생기는 단점이 있다
어떤 크기로 나눌지 신중히 결정해야 한다(내부 단편화를 줄이기 위해서)

4-4 버디 시스템
-버디 시스템의 작동 방식
큰 메모리 공간을 필요에 따라 반씩 분할하여 프로세스에 할당
사용이 끝나면 쌍(buddy)이 되는 인접 블록과 다시 병합
나뉜 메모리의 각 구역에는 프로세스가 1개만 들어감

- 버디
같은 크기
같은 부모 블록에서 분할됨
주소 차이로 쉽게 계산 가능
장점 
1. 외부 단편화 감소
2. 메모리 병합이 빠름
3. 구현이 단순함
4. 메모리 관리오버헤드 감소

단점
1. 내부 단편화 발생
2. 정확한 크기를 요청할 시 비효율적

5 분할 컴파일과 메모리 관리

5-1 분할 컴파일
여러 개 소스코드 파일로 하나의 실행 파일을 만든느 것을 다중 소스 코드라고 한다
여러 개 소스코드를 각각 컴파일해 하나의 실행 파일로 만든느 것을 분할 컴파일이라고 한다.
서로 다른 목적 코드를 하나로 만든느 것이 연결 단계

5-2 변수와 메모리 할당
-컴파일러와 변수
컴파일러는 변수를 사용할 때마다 사용 범위를 넘는지 점검
컴파일러는 모든 변수를 메모리 주소로 바꾸어 기계어로 된 실행 파일을 만듦
컴파일러에 의해 만들어진 변수의 주소는 논리 주소임
------------

chapter8. 가상 메모리

1 가상 메모리의 개요

1-1 연속 메모리 할당
프로세스에 연속적인 메모리 공간을 할당하는 방식

메모리의 연속적인 공간할당 시 고려해야 할 사항
-메모리 크기 적합성
-메모리 배치 정책
-외부 단편화 관리
-내부 단편화 가능성
-프로세스 크기 변화 가능성
-주소 재배치 및 보호 문제
-메모리 재배치
-시스템 환경 적합성과의 연계성

스와핑

스왑영역의 필요성
-메모리 한계 문제(램이 부족)
-실행 중인 프로세스 수 증가
-개별 프로그램의 메모리 사용량 증가

=> 필요없는 프로세스를 디스크로 보냄, 새로운 프로세스를 메모리에 적재(이때 사용되는 공간이 스왑영역)

메모리가 가득참 -> os가 덜 사용하는 프로세스를 선택 -> 해당 내용을 스왑 영역으로 이동 -> 빈 메모리에 새로운 프로세스 적재 -> 필요해지면 다시 메모리로 복귀

메모리 할당
연속할당
-고정분할 방식
-가변분할 방식
미연속할당(보조 -> 주기억장치)
-페이징
-세그멘테이션
-페이징_세그멘테이션

1-2 가상 메모리 시스템

-가상 메모리의 크기와 주소
프로세스가 바라보는 메모리 영역과 메모리 관리자가 바라보는 메모리 영역으로 나뉨
가상 메모리에서 메모리 관리자는 물리 메모리의 부족한 부분을 스왑 영역으로 보충

-가상 메모리
가상 메모리를 이용하면 프로세스는 운영체제가 어디에 있는지, 물리 메모리의 크기가 어느 정도인지 신경 쓰지 않고 메모리를 사용할 수 있다.
프로세스마다 독립적인 가상 주소 공간을 제공

필요성

1.문제상황(가상 메모리 미사용시)
-ram용량 제한
-한 번에 실행 가능한 프로그램 수 제한
-큰 프로그램 실행 불가

2.해결(가상 메모리 사용)
-일부만 메모리 적제
-나머지는 디스크에 저장
-필요할 때만 교체
-기본 원리
-주소 공간 분리
-필요한 부분만 적재
-주소 변환


가상메모리의 기본원리
-주소공간 분리(논리주소 : 프로세스가 사용하는 주소, 물리주소)
-필요한 부분만 적재
실행에 필요한 페이지만 RAM에 올림
사용하지 않는 부분은 디스크에 존재
-주소 변환(cpu -> 논리 주소 생성, mmu(메모리 매핑 테이블)->페이지 테이블 참조, 논리 주소->물리주소 변환)

가상메모리시스템
-동적 주소 변환(DAT): 가상 주소를 메모리의 물리 주소로 변환

메모리 매핑 테이블
-가상 메모리 시스템에서 메모리 관리자는 가상 주소와 물리 주소를 일대일 매핑 테이블로 관리
-페이징 기법 : 페이징 매핑 테이블, 세그먼테이션 기법: 세그먼테이션 매핑 테이블

지역성 : 기억장치에 접근하는 패턴이 메모리의 특정 영역에 집중되는 성질(프로그램의 흐름이랑 비슷하다)
-공간의 지역성: 현재 위치에서 가까운 데이터에 접근할 확률이 높다
-시간의 지역성: 현재를 기준으로 가장 가까운 시간에 접근한 데이터가 사용될 확률이 높다
-순차적 지역성: 작업이 순서대로 진행되는 것을 의미
예) 캐시, 페이지 교체 알고리즘


2 페이징 기법(운영체제 영역)

2-1 페이징 기법의 구현
페이징 기법
-페이지:가상 주소(0번부터 시작한다 : 논리주소)
-프레임:물리적인 기억장소
=>둘다 사이즈는 똑같다
invalid => 스왑 영역에 있다는 의미

2-2 페이징 기법의 주소변환

동적 주소 변환
프레임 번호 * 페이징 크기 + 오프셋(변위): 페이지,프레임 둘다 같다

페이징 폴트 -> 가상메모리 -> 페이징 테이블 -> 물리메모리

다수의 프로세스가 있는 페이징 시스템
-각 프로세스는 자신만의 독립된 테이블을 가짐(pcb블록)
-물리메모리는 공유될 수 있음

컨텍스트 스위치 : 문맥교환
cpu가 프로세스를 전환하면 컨텍스트 스위치가 일어남
=> 전체 무효화 되고 새 프로세스의 주소 공간으로 완전 전환

페이지 테이블 엔트리
-각 페이지에 대한 메타데이터 저장
-프레임 번호 저장

프로세스가 증가하면 페이지 테이블크기가 커지고 실제 사용할 수 있는 메모리 영역이 줄어듬
