리스트 : 여러 개의 데이터를 순서대로 나열한 형태의 자료형 []
튜플 : 리스트와 비슷하지만 수정 및 삭제가 되지 않는 불변성을 가지고 있다 () 
딕셔너리 : 키와 밸류라는 특성을 가지고 있는 자료형  순차적인 리스트에 반해 딱히 제약이 없이 불러낼수있다.{} 순서가 없고 중복이 불가
집합: 중복을 허용하지않고 순서가 없는 데이터의 집합 {}


vs code
단축키 : ctrl shift ~ => 터미널 열기


데이터를 저장할 수 있는 공간, 메모리 공간에 이름을 붙이는 것 => 변수

데이터 저장방식 -> 메모리를 효율적으로 저장하기 위한 방식 -> 자료형 : 프로그램에서 사용할 있는 데이터의 종류

파이썬은 문자열과 문자를 구분하지 않는 형태로 쓴다.



bit : 2가지(true=1, false=0)

byte : 8bit

두개의 정수를 입력받아서 두 정수의 사칙연산 출력

0으로 초기화해서 합을 구할 때 시작지점을 보기 편하게 해준다

sum = 0 + 1 + 2 + 3 + 4 + 5 (1부터 5까지의 합을 구할 때 앞에 0을 먼저 넣어주면서 편하게해준다)

for문에서 else는 for문을 다 돌고 다음에 else를 수행해야한다.


A and,or B 와 B and,or A 는 결과값이 다를 수 있다!! 
=> 어떤 걸 앞에 오게할지 고민해야 한다.


*x, = 10,20,30 => x=[10,20,30]

==============================================
2026.03.30

파일입출력

read로 통채로 들고오면 위험하다(용량이 크면 부하를 가질 수 있다)

렘에서 입출력장치 간에 이동을 스트림이라하고 char by char로 주고받는다.

flush : 버퍼안에 있는 것을 다 없애는 행위

=============================================
2026.03.31
pickle
객체(리스트,딕셔너리 ,함수, 클래스)들을 파일에다가 저장시키기 위해 필요한 것


렘에서 파일로 전송하려면 객체들을 char로 바꿔서 보내줘야한다

파일은 byte형태로 저장한다

pickling : 객체들을 byte형태로 바꿔주는 방법

unpickling : 파일에서 메모리로 갈때 원래 형태로 바꿔주는 방법

dump() : 메모리 -> file
load() : file -> 메모리

wb, rb : byte 형식으로 받는 것

=> 하지만 file의 양이많아지면 별로 효율이 좋지 않다

그래서 db와 연결해서 db차원에서 crud하는 것이 효율이 좋다


파이썬에서 데이터베이스 연결해서 사용할 때
파이썬에서 데이터를 insert해도 oracle에는 변동이 안되어 있다
=> commit을 해줘야 oracle에도 변경이 적용된다.


* 데이터베이스 sequence 생성 및 발행

-- 일련번호 생성
create sequence t_test_no
 start with 2(2부터 시작)

insert into t_test(no, id , name)
 values(t_test_no.nextval,'kim','김길동');

=============================================
2026.04.01
from mytest(파일) import hello(함수) : 이거는 파일의 있는 여러 함수,클래스 중 함수 1개만 가져온거기 때문에 다른 함수는 사용할 수 없다
=> 또 쓰려면 hello, myTest_msg 해서 둘 다 사용할 수 있게 해준다

import mytest as mt => 이름을 축약해서 쓴다
mt.hello() => as를 썼으면 이렇게 해줘야한다