2026.03.31

# 🗄️ 데이터베이스(Database) 핵심 요약 정리

## 1. 데이터베이스의 정의 및 특징
데이터베이스는 특정 조직의 업무를 수행하는 데 필요한 상호 관련된 데이터들의 집합입니다.

### 📌 데이터베이스의 정의
* [cite_start]**공유 데이터 (Shared Data)**: 특정 조직의 여러 사용자가 함께 소유하고 이용하는 공용 데이터 
* [cite_start]**통합 데이터 (Integrated Data)**: 중복을 최소화하고 통제가 가능한 중복만 허용하는 데이터 
* [cite_start]**저장 데이터 (Stored Data)**: 컴퓨터가 접근할 수 있는 매체에 저장된 데이터 
* [cite_start]**운영 데이터 (Operational Data)**: 조직의 주요 기능을 수행하기 위해 지속적으로 유지해야 하는 데이터 

### ✨ 데이터베이스의 특징
* [cite_start]**실시간 접근성**: 사용자의 요구에 즉시 응답 
* [cite_start]**계속 변화 (Continuous Evolution)**: 삽입, 삭제, 수정을 통해 최신의 정확한 상태를 유지 
* [cite_start]**내용 기반 참조**: 주소가 아닌 데이터의 값(Value)으로 참조 
* [cite_start]**동시 공유**: 여러 사용자가 동시에 동일한 데이터를 이용 가능 

---

## 2. DBMS와 스키마
* [cite_start]**DBMS**: 데이터 중복과 종속 문제를 해결하기 위한 소프트웨어로, 일관성 및 독립성을 유지합니다. 
* [cite_start]**스키마(Schema)**: DB의 구조와 제약조건을 정의한 것 (정적인 특징). 
* [cite_start]**인스턴스(Instance)**: 스키마에 따라 실제로 저장된 값 (동적인 특징). 

---

## 3. 데이터베이스 3단계 구조
사용자 관점부터 물리적 저장 관점까지 3단계로 분리하여 독립성을 보장합니다.



| 단계 | 명칭 | 설명 |
| :--- | :--- | :--- |
| **외부 단계** | 외부 스키마 | [cite_start]개별 사용자나 프로그래머 관점에서 필요한 데이터 정의  |
| **개념 단계** | 개념 스키마 | [cite_start]조직 전체의 관점, 전체 DB의 논리적 구조 및 통합 정의  |
| **내부 단계** | 내부 스키마 | [cite_start]저장 장치 관점, 실제로 데이터가 저장되는 방법 정의  |

---

## 4. 데이터 언어 및 구성 요소
* [cite_start]**정의어 (DDL)**: 스키마 정의, 수정, 삭제 
* [cite_start]**조작어 (DML)**: 데이터 삽입, 삭제, 수정, 검색 
* [cite_start]**제어어 (DCL)**: 무결성, 보안, 권한 제어, 병행 제어 

### 📊 데이터 구조 용어 비교
| 관계형 DB (Relation) | 파일 시스템 | 설명 |
| :--- | :--- | :--- |
| **속성 (Attribute)** | 필드 (Field) | [cite_start]릴레이션의 열(Column)  |
| **투플 (Tuple)** | 레코드 (Record) | [cite_start]릴레이션의 행(Row)  |
| **차수 (Degree)** | - | [cite_start]속성의 전체 개수  |
| **카디널리티 (Cardinality)** | - | [cite_start]투플의 전체 개수  |
| **도메인 (Domain)** | - | [cite_start]속성 하나가 가질 수 있는 원자 값들의 집합  |

---

## 5. 키(Key)의 종류 및 관계대수
* [cite_start]**키의 종류**: 슈퍼키, 후보키, **기본키(PK)**, 대체키, **외래키(FK)** 
* [cite_start]**관계대수 연산**: 셀렉트($\sigma$), 프로젝트($\pi$), 합집합($\cup$), 교집합($\cap$), 차집합($-$), 카티션 프로덕트($\times$), 조인($\bowtie$), 디비전($\div$) 

---

## 6. 데이터베이스 설계 및 정규화
[cite_start]데이터 중복으로 인한 **이상현상(삽입, 삭제, 수정 이상)**을 방지하기 위한 과정입니다. 

### 📐 설계 단계
1. [cite_start]**요구사항 분석** → 2. **개념적 설계**(E-R 다이어그램) → 3. **논리적 설계**(릴레이션 스키마) → 4. **물리적 설계**(SQL) → 5. **구현** 

### 🧹 정규화 (Normalization)
[cite_start]함수 종속성을 이용하여 릴레이션을 분해하는 과정입니다. 

| 정규형 | 조건 | 해결 내용 |
| :--- | :--- | :--- |
| **제1정규형 (1NF)** | [cite_start]모든 속성의 도메인이 **원자 값**으로 구성  | 다중 값 속성 제거 |
| **제2정규형 (2NF)** | [cite_start]모든 속성이 기본키에 **완전 함수 종속**  | 부분 함수 종속 제거 |
| **제3정규형 (3NF)** | [cite_start]기본키에 **이행적 함수 종속**되지 않음  | 이행적 함수 종속 제거 |

===========================================

2026.04.02

# 🔍 SQL 질의 및 데이터 활용 정리

## 1. 단순 질의 (Simple Query)
데이터베이스의 테이블에서 원하는 데이터를 추출하는 가장 기본적인 방법입니다.

* **기본 구조**: `SELECT` (열) `FROM` (테이블) `WHERE` (조건)
* **주요 키워드**:
    * `DISTINCT`: 중복된 결과 제거
    * `AS`: 열이나 테이블에 별칭(Alias) 부여
    * `ORDER BY`: 결과 정렬 (`ASC`: 오름차순, `DESC`: 내림차순)
    * `LIKE`: 패턴 일치 검색 (`%`: 모든 문자, `_`: 한 글자)

---

## 2. 집계 함수 (Aggregate Function)
여러 행의 데이터를 그룹화하여 하나의 요약된 값을 계산하는 함수입니다.

| 함수 | 의미 | 설명 |
| :--- | :--- | :--- |
| **SUM** | 합계 | 숫자 열의 총합 계산 |
| **AVG** | 평균 | 숫자 열의 평균값 계산 |
| **COUNT** | 개수 | 행의 개수 계산 (NULL 제외 가능) |
| **MAX / MIN** | 최대 / 최소 | 해당 열의 가장 큰 값과 작은 값 추출 |

> 💡 **GROUP BY & HAVING**
> * `GROUP BY`: 특정 속성을 기준으로 그룹을 나누어 집계할 때 사용
> * `HAVING`: 집계 함수 결과에 대한 조건을 걸 때 사용 (WHERE와 구분 필수)

---

## 3. 부속 질의 (Subquery)
하나의 SQL 문 안에 포함된 또 다른 SQL 문을 의미합니다.

* **종류**:
    * **중첩 질의(Nested Query)**: `WHERE` 절에 사용되는 부속 질의
    * **스칼라 부속 질의**: `SELECT` 절에 사용되어 단일 값을 반환
    * **인라인 뷰(Inline View)**: `FROM` 절에 사용되어 가상 테이블 역할을 수행
* **연산자**: `IN`, `ANY`, `ALL`, `EXISTS` 등을 사용하여 메인 쿼리와 연결

---

## 4. 조인 (Join)
두 개 이상의 테이블을 결합하여 관련된 데이터를 한꺼번에 조회하는 방식입니다.



| 조인 종류 | 특징 |
| :--- | :--- |
| **내부 조인 (Inner Join)** | 양쪽 테이블에서 조인 조건이 일치하는 데이터만 추출 |
| **외부 조인 (Outer Join)** | 조건이 맞지 않아도 한쪽 테이블의 전체 데이터를 포함 (Left, Right, Full) |
| **자체 조인 (Self Join)** | 자기 자신 테이블과 조인 (예: 직원 테이블에서 상사 이름 찾기) |
| **등가 조인 (Equi Join)** | `=` 연산자를 사용하여 정확히 일치하는 값끼리 연결 |

---

## 💡 실전 요약
1.  **단순 질의**로 데이터를 찾고,
2.  **집계 함수**로 통계를 내며,
3.  **조인**을 통해 흩어진 테이블 정보를 합치고,
4.  복잡한 조건은 **부속 질의**로 해결합니다.

=====================================

2026.04.09

# 📊 SQL 질의 응용 및 고급 테크닉 정리

## 1. SELECT 문 실행 순서와 GROUP BY 원칙
SQL 문은 작성 순서와 실제 실행 순서가 다릅니다. 이 흐름을 이해하면 쿼리 오류를 방지할 수 있습니다.

### 📝 실행 순서 및 문법
1.  **FROM**: 어디서 가져올지 결정
2.  **WHERE**: 그룹화 전, 원본 데이터에서 불필요한 행 제거 (**집계함수 사용 불가**)
3.  **GROUP BY**: 특정 '~별'로 데이터 바구니 담기
4.  **HAVING**: 그룹화된 결과(요약 정보)에 대해 조건 걸기
5.  **SELECT**: 보고 싶은 컬럼 및 요약 정보(AVG, SUM 등) 출력
6.  **ORDER BY**: 결과 정렬

> 💡 **GROUP BY의 철칙**
> "SELECT 절에 쓴 컬럼 중, 집계 함수를 제외한 모든 컬럼은 반드시 GROUP BY 뒤에 적어주어야 한다."

---

## 2. 서브쿼리(Subquery) 활용 비법
서브쿼리는 출력되는 행의 개수에 따라 사용하는 연산자가 달라집니다.

| 서브쿼리 결과 | 사용 연산자 | 특징 |
| :--- | :---: | :--- |
| **단일 행 (1줄)** | `=`, `<>`, `>`, `<` | 결과가 딱 하나일 때 사용 |
| **다중 행 (여러 줄)** | `IN`, `ANY`, `ALL` | 여러 결과 중 하나라도 맞거나(`ANY`), 모두 맞아야 할 때(`ALL`) |
| **존재 여부** | `EXISTS` | 서브쿼리에 조건에 맞는 데이터가 '있는지'만 확인 |

* **예시**: `sal > ALL (SELECT sal FROM emp WHERE job = 'CLERK')`
    * 해석: 모든 CLERK들의 급여보다 높은 경우(즉, CLERK 중 최고 급여보다 높음)

---

## 3. 상관 서브쿼리 (Correlated Subquery)
"전체 기준"이 아닌 **"일대일 맞춤형 기준"**으로 데이터를 검사하는 방식입니다.

### 🔄 작동 원리 (직원 한 명씩 붙잡고 물어보기)
1. **메인 쿼리**: 직원 한 명(예: 김철수)을 선택함.
2. **정보 전달**: 김철수의 부서 정보(예: 개발팀)를 서브쿼리로 전달함.
3. **서브쿼리 계산**: "개발팀의 평균"만 새로 계산함.
4. **비교 및 결정**: 김철수의 급여와 방금 구한 "개발팀 평균"을 비교하여 출력 여부 결정.
5. **반복**: 다음 직원을 잡고 위 과정을 반복함.



| 구분 | 일반 서브쿼리 | 상관 서브쿼리 |
| :--- | :--- | :--- |
| **기준** | 고정 기준 (전체 평균 등) | 변하는 기준 (자신이 속한 부서 평균 등) |
| **특징** | 한 번 계산하고 재사용 | 메인 쿼리 행마다 매번 새로 계산 |

---

## 4. 조인(Join) 심화: Self Join
자기 자신의 테이블을 다시 참조하여 관계를 푸는 방식입니다.

* **활용 사례**:
    1. **계층 구조 파악**: 상사-부하 직원, 카테고리-상위 카테고리 관계 조회.
    2. **동료 찾기**: "나와 같은 부서의 다른 사람"을 찾을 때 (`e1.empno <> e2.empno` 조건 필수).
    3. **데이터 선후 비교**: "어제보다 매출이 오른 상품" 등 시간적 순서 비교.

> 💡 **중복 제거 팁**: Self Join 시 `e.ename < m.ename` 같은 조건을 넣으면 동일한 쌍이 순서만 바뀌어 중복 출력되는 것을 막을 수 있습니다.

---

## 5. 유용한 SQL 꿀팁 모음

### 🔢 데이터 개수 세기 (COUNT)
* `COUNT(*)` : 데이터가 몇 줄인지 궁금할 때 (고민 말고 사용)
* `COUNT(컬럼명)` : 특정 정보가 입력된(NULL이 아닌) 데이터만 셀 때
* `COUNT(DISTINCT 컬럼명)` : 중복을 제외한 종류의 개수가 궁금할 때

### 🔝 상위 N개 출력 (TOP-N)
```sql
SELECT ename, sal
FROM emp
ORDER BY sal DESC
FETCH FIRST 3 ROWS ONLY; -- 급여 상위 3명 추출

### 💡 보충 가이드
* **GROUP BY 사용 시점**: 사원 한 명 한 명의 정보가 필요할 때는 필요 없으나, 부서**별**, 직무**별** 통계(평균, 합계 등)를 내야 할 때는 반드시 사용해야 합니다. 
* **데이터 무결성**: 집계 함수는 `WHERE` 절에 직접 쓸 수 없으며, 반드시 `HAVING` 절이나 서브쿼리를 통해 필터링해야 합니다. 

