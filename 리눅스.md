
2026.04.03

PCB
printed Circuit Board(회로 판)

SW 

firmware : 예전엔 특정 기능을 수행하는 이유로 개발되는 것 , 단순동작(특정 기능)
=> 기능이 늘어나면서 회로가 복잡해진다 =>인베디드 sw로 변한다
=> 관리가 필요한 체계가 필요해진다 => operating system(운영체계)를 만든다


 
다이오드 -> 트랜지스터 -> 집적회로(ic) -> chip

최신 운영체제
Android
IOS
windows

운영체제의 시작
unix -> MS DOS(디스크에 있는 정보를 운영): 16비트 컴퓨터(IBM과 MicorSoft) ->  Linux(오픈소스이기 때문에 보안이 점점 강화된다)

메인프레임(컴퓨터 본체)과 콘솔(모니터,키보드)

virtualization(가상화)
- clustering : 여러대의컴퓨터를 가상화해서 1대처럼 만드는 것
- 1대의 pc를 가상화해서 여러 대의 컴퓨터로 분할(이때 가상화 밑에있는 os(window)를 host라고 한다)

커널(심장:리눅스) + ui + 기능 => 상품화

distribute(배포)
배포판 중 : red hat(가장 오래됨) , ubuntu(제일 사용자가 많이씀)

Ubuntu 24.04.4 LTS(long term service) : LTS는 4년

24는 lts시작 시점

cpu : alu(산술 논리 장치) + 
central
processing
unit


switch: 전기의 신호를 1, 0으로 바꿔주는 장치
TR(트랜지스터) : 

부울대수 (bool Algebrn) : OR

* 논리식을 가지고 산술식을 만들수 있다
=> 반도체 회사에서는 not and(NAND)  => 기억장치 (NAND memory)

1개의 코어에 2가지 일을 시키는 것 =>  하이퍼 스레드

efficient-core : 코어하나당 1개의 일을 시키는 것(전기를 조금 먹는다)
performance-core : 고성능 코어(게임같이 하이퍼 스레드를 써야하는 코어)
스레드는 p-core * 2 + e-core

=======================================================

리눅스

network 

network topology(형상)을 고민하기 시작한다
ring -> mesh -> bus(중심에 하나를 놓고 각각을 연결하는 구조)

host : 네크워크 망에 달려있는 컴퓨터

network 간의 서로 컴퓨터가 정보를 주고받고싶다
=>inter network(인터넷)

네트워크 간의 통신을 하기위한 규약 :  internetwork protocol(IP)
식별하기 위해 번호 할당하는 것(고유한 주소) :  ip address(주소)

ipv4 : ip version 4
4개의 주소 체계로 되어있음
한칸이 1byte =>4byte 체계 

1byte가 표현 가능한 10진수 숫자 범위 : 256개
256.256.256.256



ip주소 : 기계가 식별하기 위한 주소(네트워크 주소 + 호스트 주소)
=> host name(사람이 쓰기 편하다) : 사람이 기억하기 쉽게 붙인 문자로 된 이름입니다, 네트워크 내의 개별 장치 이름 , 특정 서비스를 제공하는 서버 식별

domain name(www.naver.com) : 홈페이지 주소 = 인터넷 주소(url) , 네트워크 전체 영역의 이름 , 해당 조직이나 그룹의 위치 식별

컴퓨터 층구조 
Linux(host name= ubuntu2404)
가상머신
virtual box
windows 11
desktop-0000(host name)
h/w


터미널 : 모니터와 키보드 한 쌍
컴퓨터가 있고 사람이 있으면 사람과 컴퓨터간의 인터페이스 역할을 해주는 것

터미널의 앞에 있는 화면을 글씨가 나온다고 해서 prompt ( user@ubntu2024 : ~$)


internet date center(idc) : web서버 임대(이유: web sevice를 할려고)
web서버 : pc, network를 webservice에 특화해서 임대해준다 : hosting

web service에 올리는 sw : 프론트 , 백엔드

linux

directory : window에서는 folder

=> directory도 파일의 한 종류이다

file system

1. directory 
2. file


/ : root directory
~ : /home/user/

ls  : list (폴더)
ls -l : (폴더의 자세한 내용: 권한 + 날짜 + 생성자 등) 
cd : change directory
mkdir : make directory
clear
chmod : 권한 바꾸기


커널 : 자원 관리

시스템 명령어 = 시스템 소프트웨어

root 권한 = super user 권한

drwx

directory
read
write
excution

d rwx rwx rwx (소유자 권한, 그룹 권한, 기타 사용자 권한) : 777

소유자 권한 : 파일을 만들 때 사용한 아이디의 권한


vi : 편집기 (시스템 명령어는 아님)
보기 상태, 편집 상태

q! : 나가기
wq : 쓴 내용 저장 후 나가기

vim은 설치를 해줘야한다

sudo : super user doing
apt : 프로그램 설치하는 소프트웨어
install : 설치
