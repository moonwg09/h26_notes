데이터 베이스

데이터베이스의 정의
공유 데이터 : 특정 조직의 여러 사용자가 함께 소유하고 이용할 수 있어야 하는 공용 데이터
통합 데이터 : 데이터의 중복을 최소화하고 통제가 가능한 중복만 허용하는 데이터
저장 데이터 : 컴퓨터가 접근할 수 있는 매체에 데이터 베이스를 저장해야한다.
운영 데이터 : 지속적으로 유지해야하는 데이터(조직을 운영하고 조직의 주요 기능을 수행하기 위해 필요하다)

데이터베이스의 특징
실시간 접근성: 사용자의 데이터 요구에 실시간으로 응답해야한다
계속 변화 : 동적인 특징이 있어 데이터를 삽입,삭제,수정하여 현재의 정확한 데이터를 유지한다
내용 기반 참조 : 저장된 주소나 위치가 아닌 데이터의 내용(값)으로 참조할 수 있다.
동시 공유 : 여러 사용자가 동시에 이용할 수 있는 동시 공유의 특징을 제공(같은 데이터를 동시에 사용하는 것도 지원한다)


DBMS : 데이터 중복과 데이터 종속 문제를 해결하기 위해 제시된 소프트웨어
데이터 중복 최소화
데이터 일관성 유지(중복 제거)
데이터 독립성 유지
프로그램 개발 생산성 향상

스키마 : 데이터베이스에 저장되는 데이터 구조와 제약조건을 정의한 것(자주 변경되지 x)
인스턴스 : 스키마에 따라 데이터베이스에 실제로 저장된 값(계속 변한다)

시스템 구성 

데이터베이스의 3단계 구조 (외부단계, 개념 단계, 내부 단계)

외부 단계 : 개별 사용자 관점에서 이해하고 표현하는 단계
=> 외부 스키마 : 사용자에게 필요한 데이터베이스를 정의

개념 단계 : 조직 전체의 관점에서 이해하고 표현하는 단계
=> 개념 스키마: dbms나 관리자의 관점에서 모든 사용자에게 필요한 데이터를 통합하여 전체 데이터베이스의 논리적 구조를 정의

내부 단계 : 저장 장치의 관점에서 이해하고 표현하는 단계( 실제로 저장되는 방법 : 레코드 길이 등)
=> 내부 스키마: 전체 데이터베이스가 저장 장치에 실제로 저장되는 방법을 정의


데이터 언어 

정의어: DDL 스키마를 정의하거나, 수정 또는 삭제하기 위해서 사용
조작어: DML 데이터의 삽입, 삭제, 수정,검색 처리를 요구하기 위해서 사용
제어어: DCL 동시 공유가 가능하면서도 무결성과 일관성을 유지하도록 내부적으로 필요한 규칙이나 기법들을 정의하기 위해서 사용

개체(entity) => record
관계(relationship)
속성(attribute) => field

릴레이션 : 테이블(table) => 파일시스템에서는 file
속성 : 릴레이션의 열 => 파일시스템에서는 field
투플 : 릴레이션의 행 => 파일시스템에서는 record
차수 : 하나의 릴레이션에서 속성의 전체 개수
카디널리티: 하나의 릴레이션에서 투플의 전체 개수
도메인 : 속성 하나가 가질수 있는 모든 값의 집합을 해당 속성의 도메인이라고 한다.( int, char 등)

슈퍼키
기본키 (primary key)
외래키(foreign key)
후보키
대체키



관계대수: 릴레이션에서 원하는 결과를 얻기 위해 대수적 연산을 이용하는 질의 언어
=> 연산 결과로 새로운 릴레이션 도출

관계대수 연산자
셀렉트 		: R1에서 조건에 맞는 튜플을 추출한다.
프로젝트 		: R2에서 조건에 맞는 속성만 추출한다.
디비전 		: 
조인 			:
합집합 		: R1과 R2의 합집합을 구한다.
교집합 		: R1과 R2의 교집합을 구한다.
차집합		: R1과 R2의 차집합을 구한다.
카티션 프로덕트	: r1 X r2 r1에 속한 각 투플과 r2에 속한 각 투플을 모두 연결하여 만드러진 릴레이션

데이터베이스 설계

1 요구사항 분석 : 요구사항 명세서
2 개념적 설계 : e-r 모델을 e-r다이어그램으로 표현
3 논리적 설계 : e-r 다이어그램을 릴레이션 스키마로 변환=> 테이블 명세서
4 물리적 설계 : 물리적 스키마(sql문을 작성)
5 구현

다대다 관계는 릴레이션으로 변환
일반적인 일대다 관계는 외래키로 표현
다중 값 속성은 릴레이션으로 변환


데이터베이스 생성

이상현상
1. 삽입시 : 새 데이터를 삽입하기 위해 불필요한 데이터도 함께 삽입하는 문제
2. 삭제시 : 중복 투플 중 일부만 변경하여 데이터가 불일치하게 되는 모순의 문제
3. 수정시 : 투플을 삭제하면 꼭 필요한 데이터까지 함께 삭제되는 데이터 손실의 문제

함수 종속성 

정규화: 함수 종속성을 이용하여 릴레이션을 연관성이 있는 속성들로만 구성되도록 분해해서, 이상현상이 발생하지 않는
          올바른 릴레이션으로 만드는 과정

정규화(1정규형 -> 2정규형 -> 3정규형)


1정규형 : 릴레이션의 속한 모든 속성의 도메인이 원자 값으로만 구성되어 있으면 제 1정규형에 속한다
2정규형 : 기본키가 아닌 모든 속성이 기본키에 완전 함수 종속되면 제 2정규형에 속한다.(부분 함수 종속을 제거)
3정규형 : 기본키가 아닌 모든 속성이 기본키에 이행적 함수 종속이 되지 않으면 제3정규형에 속한다.

===========================================

2026.04.02

데이터베이스

단순질의

부속질의

집계함수

조인


=====================================

2026.04.09

# 서브쿼리의 행이 하나만 출력되는 경우에는 = , 여러 개가 출력되는 경우에는 in을 사용한다.

# GROUP BY job: 업무(job)가 같은 사람들끼리 바구니에 담는다고 생각하면 됩니다. (예: SALESMAN끼리, CLERK끼리)

# ename != 'jones' : jones는 제외 

SELECT   부서번호, AVG(급여)  -- 3. 보고 싶은 요약 정보
FROM     사원테이블          -- 1. 어디서 가져올지
WHERE    급여 > 1000        -- (선택) 바구니 담기 전에 미리 버릴 사람
GROUP BY 부서번호            -- 2. 무슨 '별'로 묶을지
HAVING   AVG(급여) > 2000;   -- (선택) 요약 결과가 기준에 맞는 바구니만 남기기

문제를 읽을 때 **'~별'**이라는 단어를 찾으세요.

그 단어가 바로 GROUP BY 뒤에 올 컬럼입니다.

그 앞에 나오는 '급여의 합', '사원 수' 같은 말이 바로 SELECT 절에 쓸 함수입니다.

where 절에는 집계함수를 쓰면 안된다

union all : 두 테이블을 합친다 , 옆으로 붙이는 JOIN이 아니라, 위아래로 쌓는 UNION을 쓰는 게 정석입니다.

"전체 평균 급여": SELECT AVG(sal) FROM emp

"부서별 평균 급여": SELECT AVG(sal) FROM emp GROUP BY deptno

group by를 하게 되면 여러 개가 나온다

여러 줄을 합쳐서(집계해서) 한 줄로 만들기 위해서 -> group by
사원 한 명 한 명(각 행) -> group by 필요 없음

단순히 특정 그룹의 데이터 리스트가 필요한 거라면 GROUP BY는 필요 없습니다.

그 그룹의 **통계(평균, 합계, 최대 등)**를 내서 비교해야 할 때만 GROUP BY를 씁니다.

where sal >ALL(
    select sal
    from emp
    where job = 'CLERK'
); 
=> 모든 (all)을 써야하는 경우


EXISTS는 보통 상관 서브쿼리(Correlated Subquery) 방식으로 작동합니다. 메인 쿼리의 한 줄을 잡고 "얘가 조건에 맞나?" 하고 서브쿼리 안으로 던져주는 방식이죠.

이때 **"지금 메인 쿼리에서 가져온 그 녀석이야!"**라고 가리키는 손가락 역할이 바로 **별칭(e.empno)**입니다.

급여 상위 3위 안에 드는 사원의 이름과 급여를 출력하시오.
select ename, sal
from emp
order by sal desc
fetch first 3 rows only;


① 계층 구조를 파악할 때 (가장 대표적)
조금 전 풀어보신 것처럼 상사-부하 직원, 멘토-멘티, 카테고리-상위 카테고리 관계를 풀 때 사용합니다.

활용: "각 카테고리의 상위 메뉴 이름이 무엇인가?"

② 동일한 특징을 가진 '동료'를 찾을 때
"나와 같은 부서에 근무하는 사람들은 누구지?"처럼 기준은 같지만 나는 아닌 데이터를 찾을 때 씁니다.

SQL
SELECT e1.ename AS 나, e2.ename AS 내동료
FROM emp e1
JOIN emp e2 ON e1.deptno = e2.deptno AND e1.empno <> e2.empno;
-- (empno가 다르다는 조건을 넣어야 '나'를 제외한 진짜 동료만 나옵니다!)
③ 데이터의 선후 관계를 비교할 때
"이전 주문보다 금액이 더 큰 주문을 찾고 싶을 때"처럼 같은 테이블 내의 시간적 순서를 비교할 때 활용합니다.

활용: "어제보다 오늘 매출이 오른 상품은?"


"그냥 데이터가 몇 줄인지가 궁금해" -> 고민 말고 **COUNT(*)**를 쓰세요.
특정 정보가 입력된 데이터만 세고 싶어" -> **COUNT(컬럼명)**을 쓰세요.
"중복을 제외하고 종류가 몇 개인지 궁금해" -> **COUNT(DISTINCT 컬럼명)**을 쓰세요.


select에서 뽑아야 하는 내용이 다른 테이블에 있으면 무조건 join을 해줘야한다 !!!!!


"SELECT 절에 쓴 컬럼 중, 집계 함수(COUNT, SUM 등)를 뺀 나머지는 모두 GROUP BY 뒤에 적어줘야 한다."

self join : WHERE e.ename < m.ename 쓰면 중복 제거 된다.


last_name <> 'De Haan'; -- 본인은 제외

GROUP BY를 쓰면: 여러 줄을 합쳐서 딱 한 줄로 만듭니다. (10번 부서원 5명이 1줄로 압축됨)

group by : 출력값이 2개인데 만약 그 중 하나가 고유한값(중복이 되지 않는 값) 이면 쓰면안된다 
=> 서브쿼리를 사용해서 풀어야한다

상관 서브쿼리 : 

이 방식은 표를 미리 만들지 않고, 직원 한 명 한 명씩 붙잡고 물어보는 방식입니다.

1단계: 메인 쿼리에서 '김철수'라는 직원을 딱 잡습니다. (김철수는 개발팀입니다.)

2단계: 서브쿼리(괄호 안)로 들어갑니다. 이때 김철수의 부서(개발팀) 정보를 들고 들어갑니다.

3단계: 서브쿼리는 "아, 개발팀 평균만 내면 되지?" 하고 개발팀의 평균값 하나만 딱 구합니다.

4단계: 이제 김철수의 월급과 그 평균값을 비교해서 높으면 합격, 낮으면 탈락시킵니다.

5단계: 그다음 직원인 '이영희'를 잡고 이 과정을 반복합니다.


상관 서브쿼리(Correlated Subquery)는 이름은 거창하지만, 사실 **'일대일 맞춤형 질문'**이라고 생각하면 아주 쉽습니다.
이렇게 한 명 한 명(메인 쿼리) 확인할 때마다, 그 사람의 정보(부서번호)를 들고 가서 서브쿼리를 새로 계산하는 것이 상관 서브쿼리의 핵심입니다.

일반 서브쿼리 (고정 기준): "전체 평균보다 많이 받는 사람?"

기준은 '전체 평균' 딱 하나입니다. 누가 와도 이 기준은 안 변합니다.

상관 서브쿼리 (변하는 기준): "자신이 속한 부서 평균보다 많이 받는 사람?"

기준이 영업부였다가, 개발부였다가, 인사부였다가... 누구를 검사하느냐에 따라 기준이 계속 변합니다.

"자신이 속한 부서의 평균보다..."

"동일한 직무를 가진 사원들 중에서..."

"각 부서 내에서 급여가 가장 높은..." 

"해당 사원의 직무 이력이 존재하는지..." 

